bind

Например, есть оператор= и инт, позволяет сделать предикат "равен этому инту"

int f(int, int, int);

bind(&f, 1, 2, 3)(); == f(1, 2, 3);
bind(&f, 4, _1, 5)(6); == f(4, 6, 5);
bind(&f, _1, _2, _1)(7, 8); == f(7, 8, 7);

bind(&f, bind(&g, _1, _2), _1, _2)(9, 10); == f(g(9, 10), 9, 10);